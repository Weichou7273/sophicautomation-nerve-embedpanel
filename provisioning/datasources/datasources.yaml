apiVersion: 1

# datasources:
#   - name: Static
#     type: marcusolsson-static-datasource
#     access: proxy
#     isDefault: true
#     orgId: 1
#     version: 1
#     editable: true

datasources:
  - name: RestAPI
    type: prometheus
    url: http://localhost:3003/
    access: proxy
    isDefault: false
    jsonData:
      httpMethod: GET

